apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: logs-output-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: logs-output
                port:
                  number: 2345
